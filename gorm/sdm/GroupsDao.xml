<?xml version="1.0" encoding="UTF-8"?>
<dao-class xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:noNamespaceSchemaLocation="dao.xsd">

    <crud dto="gorm-Group" table="groups">
        <create/>
        <read/>
        <update/>
        <delete/>
    </crud>

    <query-dto-list method="GetAllGroups" dto="GroupLi"/>

    <!-- testing -->
    <query-dto dto="GroupLi" method="GetGroupLi(int64 g_id)" ref="get_group.sql"/>

    <!-- testing -->
    <query-list method="GetGroupLIIds()" ref="get_all_groups.sql" return-type="int64"/>

    <!-- testing -->
    <query method="GetGroupLIId(g_id)" ref="get_group.sql" return-type="int64"/>

    <!-- testing -->
    <exec-dml method="deleteGroup2(g_id)" ref="delete from groups where g_id=?"/>

</dao-class>